# Example Frontend

A very simple React frontend that can be run locally with a shell script.

## Run locally

```bash
./run.sh
```

Optionally choose a different port:

```bash
./run.sh 8080
```

Then open `http://localhost:5173` (or your chosen port) in a browser.

## Check

Run the lightweight repository check with:

```bash
./check.sh
```
